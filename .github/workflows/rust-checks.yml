# Copyright 2024-2026 Lager Data
# SPDX-License-Identifier: Apache-2.0
#
# Build validation for box/oscilloscope-daemon, which had none.
#
# That crate is not a side project: docker/start-services.sh launches it on box
# boot whenever /usr/local/bin/oscilloscope-daemon is present, and it drives the
# PicoScope over the vendor SDK's FFI. It is runtime code on customer hardware,
# and until this workflow no job in this repo referenced cargo at all.
#
# It binds loopback only (127.0.0.1:8085 plus a Unix socket) and is reached from
# outside through the box HTTP server's relay on :9000. Earlier versions
# published QUIC/WebTransport listeners on 8082-8084 directly; that is gone.
#
# The gap was not theoretical. Dependabot PR #172 bumped 20 crates across 22
# breaking-version boundaries, showed a GREEN tick from twelve checks, and
# failed to compile in 74 places -- because all twelve checks were Python. It
# broke two ways independently: bindgen 0.69 -> 0.72 made the generated
# PicoScope FFI bindings unparseable (70 errors), and tungstenite 0.20 -> 0.30
# changed Message::Text to take Utf8Bytes instead of String (4 errors, needing
# real source edits Dependabot cannot make). Neither was visible without
# building it by hand.
#
# Each check below was measured on this tree before being added, so a red run
# means the change under review broke something rather than that the baseline
# was already dirty:
#
#   cargo check --workspace --all-targets   clean
#   cargo clippy -W clippy::correctness     0 findings
#   cargo audit                             0 vulnerabilities
#   cargo fmt --check                       48 files differ -- NOT gated, see below
#
# `cargo check` rather than `cargo build`: it type-checks the whole workspace
# without linking, which is what catches API breaks without needing to resolve
# every link-time symbol.
#
# It DOES need the PicoScope SDK regardless. daemon/build.rs runs bindgen
# against /opt/picoscope/include/libps2000/ps2000.h unconditionally -- there is
# no feature flag that skips it -- so a build script failure stops the check
# before any Rust is looked at. The first run of this workflow proved that:
# `wrapper.h:2:10: fatal error: 'ps2000.h' file not found`. The SDK install
# below is the same one build_daemon.sh documents for the box.

name: "PR Gate: Rust Checks"

on:
  push:
    branches: [main]
    paths:
      - 'box/oscilloscope-daemon/**'
      - '.github/workflows/rust-checks.yml'
  pull_request:
  workflow_dispatch:

# The pull_request trigger is deliberately NOT path-filtered, and there is
# deliberately NO companion "stub" workflow. A path-filtered workflow does
# not report at all when paths do not match, so it can never be a required
# context; and the tempting fix -- a second always-green workflow exposing a
# job with the same name -- is unsafe, because two check runs sharing a
# context name race, and the ruleset can be satisfied by the stub while the
# real job fails. Instead this SINGLE job detects rust changes itself: the
# heavy steps run only when box/oscilloscope-daemon/ (or this file) changed,
# and otherwise the job succeeds via skip -- one check run, whose green is
# correct by construction either way. Python-only PRs pay ~20 seconds for
# checkout + detect, not the toolchain install.
#
# `push` keeps its path filter: push-to-main is not a required context, so
# there is nothing to report on non-rust pushes and no reason to spend the
# runner minutes.

permissions:
  contents: read

jobs:
  rust-checks:
    name: rust (oscilloscope-daemon)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: box/oscilloscope-daemon

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # No job here pushes anything; do not persist the token into
          # the checkout's config (zizmor: artipacked).
          persist-credentials: false
          # Full history: the detect step below needs the merge base with the
          # target branch to compute what this PR actually changes.
          fetch-depth: 0

      - name: Detect rust changes
        id: detect
        # On pull_request, diff the merge base against HEAD (the PR merge
        # commit) for the rust tree and this workflow file. On push and
        # dispatch, always run everything: push is already path-filtered,
        # and a manual dispatch means a human wants the full result.
        working-directory: ${{ github.workspace }}
        run: |
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            base=$(git merge-base HEAD "origin/${GITHUB_BASE_REF}")
            if git diff --name-only "$base" HEAD -- \
                box/oscilloscope-daemon/ .github/workflows/rust-checks.yml \
                | grep -q .; then
              changed=true
            else
              changed=false
            fi
          else
            changed=true
          fi
          echo "rust_changed=$changed" >> "$GITHUB_OUTPUT"
          echo "rust_changed=$changed (event: $GITHUB_EVENT_NAME)"
        shell: bash

      # daemon/build.rs generates FFI bindings from the PicoScope headers with
      # bindgen, unconditionally, for every family the daemon supports. Without
      # them the build script panics and nothing downstream is checked at all,
      # so this is a prerequisite rather than an optional extra. Same repo and
      # packages build_daemon.sh documents for setting up a box.
      #
      # All five families are needed, not just libps2000: the daemon generates
      # a binding set per family so one binary serves whichever driver a given
      # box has. libps6000a is installed for its headers only -- libps3000a's
      # PicoDeviceStructs.h includes PicoConnectProbes.h, which PicoTech ships
      # under the 4000a/6000a families rather than with 3000a, and build.rs
      # adds the sibling include directories to resolve it.
      #
      # libclang-dev is bindgen's own requirement (it parses the C headers with
      # libclang); pkg-config and build-essential match build_daemon.sh.
      #
      # NOTE: this makes the job depend on an external apt repo
      # (labs.picotech.com). If that host is down, this fails for a reason that
      # has nothing to do with the change under review. If that turns out to
      # happen in practice, split this job so the SDK-free protocol crate still
      # gates while the daemon check degrades to advisory.
      - name: Install PicoScope SDK and build deps
        if: steps.detect.outputs.rust_changed == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libclang-dev
          wget -qO - https://labs.picotech.com/debian/dists/picoscope/Release.gpg.key \
            | sudo gpg --dearmor -o /usr/share/keyrings/picoscope-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/picoscope-archive-keyring.gpg] https://labs.picotech.com/debian/ picoscope main" \
            | sudo tee /etc/apt/sources.list.d/picoscope.list
          sudo apt-get update
          sudo apt-get install -y \
            libps2000 libps2000a libps3000a libps4000a libps5000a libps6000a
          # Assert every header build.rs opens, so a missing one fails here
          # with the name rather than inside a bindgen panic.
          missing=0
          for header in \
              libps2000/ps2000.h \
              libps2000a/ps2000aApi.h \
              libps3000a/ps3000aApi.h \
              libps4000a/ps4000aApi.h \
              libps5000a/ps5000aApi.h \
              libps4000a/PicoConnectProbes.h; do
            if [ ! -f "/opt/picoscope/include/$header" ]; then
              echo "MISSING /opt/picoscope/include/$header"
              missing=1
            fi
          done
          [ "$missing" -eq 0 ] || {
            echo "headers missing after install -- daemon/build.rs will panic"
            exit 1
          }
          echo "all PicoScope headers present"
        shell: bash

      # Pinned, for the same reason shellcheck is pinned in static-checks.yml:
      # the clippy and audit baselines above were measured against a known
      # toolchain, and `stable` moving under us would turn this red with no
      # change in the repo. The workspace declares edition 2024, which needs
      # 1.85+; this is well past that.
      - name: Install Rust toolchain
        if: steps.detect.outputs.rust_changed == 'true'
        run: |
          rustup toolchain install 1.95.0 --profile minimal --component clippy
          rustup default 1.95.0
          rustc --version
          cargo --version
          cargo clippy --version
        shell: bash

      - name: Cache cargo registry and target
        if: steps.detect.outputs.rust_changed == 'true'
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            box/oscilloscope-daemon/target
          key: cargo-${{ runner.os }}-${{ hashFiles('box/oscilloscope-daemon/Cargo.lock') }}
          restore-keys: cargo-${{ runner.os }}-

      # The check that would have caught #172. --all-targets so tests, benches
      # and examples are type-checked too, not just the library and binaries.
      - name: cargo check
        if: steps.detect.outputs.rust_changed == 'true'
        run: cargo check --workspace --all-targets --locked
        shell: bash

      # --locked above is load-bearing: it fails if Cargo.lock would need
      # updating to satisfy the manifests, which is how a PR that edits a
      # Cargo.toml without regenerating the lockfile gets caught. Asserted
      # separately here so the failure reason is unambiguous when it happens.
      - name: Cargo.lock is in sync with the manifests
        if: always() && steps.detect.outputs.rust_changed == 'true'
        run: cargo metadata --locked --format-version 1 > /dev/null
        shell: bash

      # Added when the workspace had zero tests, on the reasoning that the
      # gate should exist before the first #[test] rather than after. It now
      # runs ~130 of them: LSCP frame encode/decode round-trips, capability
      # detection over recorded GetUnitInfo responses, the measurement maths,
      # and the modern-API driver against a mock scope. All pure logic -- no
      # hardware, which is why they can gate here.
      - name: cargo test
        if: always() && steps.detect.outputs.rust_changed == 'true'
        run: cargo test --workspace --locked
        shell: bash

      # Correctness lints only -- the tier that flags real bugs, not style.
      # Measured at 0 on this tree. The full default ruleset is 22 findings
      # (redundant_pattern_matching, unnecessary_cast, ...), which would be
      # noise rather than a gate. Same reasoning as `ruff --select E9,F63,F7,F82`
      # and `shellcheck -S error` elsewhere in this repo: start at a level that
      # is already clean so the gate means something from day one, then ratchet.
      - name: cargo clippy (correctness only)
        if: always() && steps.detect.outputs.rust_changed == 'true'
        run: cargo clippy --workspace --all-targets -- -A clippy::all -W clippy::correctness -D warnings
        shell: bash

      # RustSec advisories against Cargo.lock. This is NOT redundant with
      # Dependabot: Dependabot reads the GitHub Advisory Database, and RustSec
      # advisories reach it only once imported. Adding this check found
      # RUSTSEC-2026-0204 (invalid pointer dereference in crossbeam-epoch
      # 0.9.18) at a moment when Dependabot reported zero open alerts. That is
      # fixed in this same change, so this step starts clean.
      #
      # Fails on vulnerabilities. Does NOT fail on `warning` classes
      # (unmaintained, unsound), of which this tree currently has three:
      # rustls-pemfile unmaintained (RUSTSEC-2025-0134, two versions in the
      # graph) and anyhow unsound Error::downcast_mut (RUSTSEC-2026-0190).
      # Neither has a fixed version to move to today. Add --deny warnings once
      # they clear.
      - name: cargo audit
        if: always() && steps.detect.outputs.rust_changed == 'true'
        run: |
          cargo install cargo-audit --locked --version 0.22.2
          cargo audit
        shell: bash

      # NOT gated: `cargo fmt --check` reports 48 files on this tree, so it
      # would be a permanently-red advisory check, and this repo's position is
      # that a check people learn to ignore is worse than no check. Reported to
      # the job summary instead, so the number is visible and can be worked
      # down deliberately.
      - name: Formatting (report only)
        if: always() && steps.detect.outputs.rust_changed == 'true'
        run: |
          rustup component add rustfmt
          n=$(cargo fmt --all --check 2>/dev/null | grep -c '^Diff in' || true)
          {
            echo "### Rust formatting"
            echo
            echo "\`cargo fmt --all --check\`: **${n}** files differ."
            echo
            echo "Not gated -- see the comment in .github/workflows/rust-checks.yml."
          } >> "$GITHUB_STEP_SUMMARY"
        shell: bash

      # Push-to-main only: proves the release profile actually compiles and
      # links (check/clippy prove the debug profile without linking) and
      # stages the binary as an artifact. Header comment discipline: this is
      # a COMPILE SMOKE on ubuntu-latest, NOT a ship artifact -- the deployed
      # daemon is built by build_daemon.sh against the box's own OS/glibc and
      # distributed by hand. Making CI produce the real ship artifact needs a
      # build container matching the box image; recorded as future work.
      - name: cargo build --release (compile smoke, push only)
        if: github.event_name == 'push' && steps.detect.outputs.rust_changed == 'true'
        run: cargo build --release --workspace --locked
        shell: bash

      - name: Upload release-profile daemon binary (push only)
        if: github.event_name == 'push' && steps.detect.outputs.rust_changed == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: oscilloscope-daemon-release-smoke-${{ github.sha }}
          path: box/oscilloscope-daemon/target/release/daemon
          retention-days: 30
          if-no-files-found: error

      # Always runs, so a green check is auditable: the log says which arm
      # produced it.
      - name: Verdict
        if: always()
        working-directory: ${{ github.workspace }}
        env:
          RUST_CHANGED: ${{ steps.detect.outputs.rust_changed }}
        run: |
          if [ "$RUST_CHANGED" = "true" ]; then
            echo "Rust paths changed: the step results above are the real result."
          else
            echo "No rust paths changed: success via skip, correct by construction."
          fi
        shell: bash
