{
  "name": "mintlify-lager-docs",

  "//": "Pinned to the same mint version static-checks.yml pins, so a local run and CI cannot disagree. Bump both together.",
  "scripts": {
    "dev": "npx --yes mint@4.2.827 dev",
    "validate": "npx --yes mint@4.2.827 validate",
    "broken-links": "npx --yes mint@4.2.827 broken-links --files 'source/**/*.mdx' --check-anchors"
  },
  "dependencies": {},
  "engines": {
    "node": "20.x"
  }
}
