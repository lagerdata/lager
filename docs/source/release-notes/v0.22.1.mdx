---
title: "Version 0.22.1"
description: "June 2, 2026"
---

## <u>Improvements</u>

- **Standardized Lager Box references to placeholders across the repository.** All `--help` output, command docstrings, source comments, the CHANGELOG, release notes, and documentation now use the `<BOX>` placeholder (and `<box-ip>`) for box names and addresses; test fixtures use a neutral `test-box` token. This is a documentation/metadata change only — no functional or API changes.

## <u>Installation</u>

To install this version:

```bash
pip install lager-cli==0.22.1
```

To upgrade from a previous version:

```bash
pip install --upgrade lager-cli
```

## Resources

[View Release on PyPI](https://pypi.org/project/lager-cli/0.22.1/)
